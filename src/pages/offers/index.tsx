import type { NextPage } from "next";

import { Layout } from "../../components/Layout";

const Offers: NextPage = () => {
  return <Layout>{() => <div>PAge</div>}</Layout>;
};

export default Offers;
